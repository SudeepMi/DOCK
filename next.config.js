module.exports = (phase, { defaultConfig }) => {
 
  return {
    /* config options for all phases except development here */
    // images: {
    //     loader: 'imgix',
    //   }
    // exportpathmap
    // exportTrailingSlash
    // exportPathMap
}
}
